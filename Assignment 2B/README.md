## Installing dependencies
Before running the commands, ensure your working directory is set to the root folder of the project, the one that contains the main subfolders `data/`, `models/`, and `test/`. Open your terminal or command prompt, and execute the following:
```
pip install -r requirements.txt
```

## Executing the program
To run the GUI, execute the following command:
```
python main.py
```